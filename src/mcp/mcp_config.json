{
  "mcpServers": {
    "disaster-response": {
      "command": "python3",
      "args": ["-m", "src.mcp.server"],
      "env": {
        "BACKEND_URL": "http://localhost:8000",
        "PYTHONPATH": "${workspaceFolder}"
      }
    }
  }
}
